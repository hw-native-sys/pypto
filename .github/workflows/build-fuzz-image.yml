name: Build Fuzz CI Image

on:
  push:
    branches: [main, fuzz]
    paths: [".github/docker/fuzz-ci.Dockerfile"]
  workflow_dispatch:

env:
  IMAGE: ghcr.io/${{ github.repository }}/fuzz-ci

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .github/docker
          file: .github/docker/fuzz-ci.Dockerfile
          push: true
          tags: ${{ env.IMAGE }}:latest
