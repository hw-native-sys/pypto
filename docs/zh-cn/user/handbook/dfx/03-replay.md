# DFX 功能：回放

> **状态：** 草稿骨架。从已有 `build_output` 重新运行，无需重新编译。

## 何时使用

_TODO —— 复现一次已捕获的运行、分享复现、重新渲染诊断。_

## 步骤

_TODO —— 让 runner 指向已有 `build_output`；哪些被复用、哪些被重新生成。_

## 参见

- 开发者参考：[`dev/03-runtime-dfx.md`](../../../dev/03-runtime-dfx.md)（回放 `build_output`）
